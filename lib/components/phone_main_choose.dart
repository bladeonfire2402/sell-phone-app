import "package:flutter/material.dart";

class PhoneMainChoose extends StatelessWidget {
  final String ImageStringText;
  final String ProductName;
  final int ProductPrice;

  const PhoneMainChoose({
    Key? key,
    required this.ImageStringText,
    required this.ProductName,
    required this.ProductPrice
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
